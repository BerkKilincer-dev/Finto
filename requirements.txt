# Finto — Python bağımlılıkları (sesli asistan + AI servisleri)
# Kurulum: pip install -r requirements.txt
#
# Not:
# - Erişilebilir mod, klavye kısayolları ve komut paleti frontend tarafında çalışır,
#   ek bir Python paketi gerektirmez.
# - Sesli asistan ve Gemini akışı için aşağıdaki paketler zorunludur.

# HTTP / altyapı
httpx>=0.28.0
python-dotenv>=1.0.0

# Voice + AI
websockets>=13.0
google-genai>=1.29.0

# Opsiyonel servis katmanı (ileride REST health/check endpointleri için)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
